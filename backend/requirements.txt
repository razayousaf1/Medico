# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# ---- Settings / validation ----
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# ---- HTTP client (Gemini API) ----
httpx>=0.27,<1.0

# ---- Firebase Firestore ----
firebase-admin>=6.5,<7.0

# ---- Dataset importer ----
openpyxl>=3.1,<4.0
requests>=2.32,<3.0
python-slugify>=8.0,<9.0

# ---- OCR (PaddleOCR) — optional, not installed on Railway ----
# Install locally with: pip install -r requirements-ocr.txt
# OCR is lazy-loaded; the API degrades gracefully when not installed.
