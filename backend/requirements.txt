# ---- Web framework ----
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
python-multipart>=0.0.9

# ---- Settings / validation ----
pydantic>=2.7,<3.0
pydantic-settings>=2.2,<3.0

# ---- HTTP client (Gemini API) ----
httpx>=0.27,<1.0

# ---- Firebase Firestore ----
firebase-admin>=6.5,<7.0

# ---- OCR (PaddleOCR) ----
# First run downloads detection/recognition models (~100 MB) to ~/.paddleocr
paddleocr>=3.0,<4.0
paddlepaddle>=3.0,<4.0

# ---- PDF rendering for OCR ----
pymupdf>=1.24,<2.0
